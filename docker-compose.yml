version: '2'
services:
  approvalservice:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8082:8082"
    restart: unless-stopped
    volumes:
      - '.:/usr/src/app'
    working_dir: /usr/src/app